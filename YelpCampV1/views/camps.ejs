<h1>This is the camp page</h1>

<a href="/campgrounds/new">Add new Camp</a>

<% camps.forEach((camp) =>{ %>
  <div>
    <h4><%= camp.title %></h4>
    <img src="<%= camp.image %>" alt="">
  </div>
<% }); %>
